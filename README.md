# `megane-modules/build`
build tools for megane modules

### First Setup
```sh
curl -O "https://bai.lol/megane/setup"
chmod +x setup
./setup [version] [project_name]
```

### Building
```sh
./setup
./gradlew build
```
